% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panelClasses.R
\name{detailMap}
\alias{detailMap}
\title{Create a detail map pane}
\usage{
detailMap(
  id = NA,
  currentCol = 1,
  currentRow = 1,
  colZoomLevel = 1,
  dataBoxHeight = 42,
  dataBoxWidth = 42,
  dataPerCol = 12,
  dataPerRow = 12,
  mode = "NORMAL",
  rowZoomLevel = 1,
  selectedIsDendrogram = FALSE,
  selectedStart = 0,
  selectedStop = 0,
  version = "P",
  versionNumber = 1
)
}
\arguments{
\item{id}{Character string identifying the detail map pane. Must be specified, no default.}

\item{currentCol}{Integer. Current column position in heatmap (must be > 0). Default is 1.}

\item{currentRow}{Integer. Current row position in heatmap (must be > 0). Default is 1.}

\item{colZoomLevel}{Numeric. Column zoom level between 0 and 1. Default is 1.}

\item{dataBoxHeight}{Integer. Height of each data box. Must be one of the predefined sizes. Default is 42.}

\item{dataBoxWidth}{Integer. Width of each data box. Must be one of the predefined sizes. Default is 42.}

\item{dataPerCol}{Integer. Number of rows displayed in detail map (must be > 1). Default is 12.}

\item{dataPerRow}{Integer. Number of columns displayed in detail map (must be > 1). Default is 12.}

\item{mode}{Character. View mode: "NORMAL", "RIBBONH", or "RIBBONV". Default is "NORMAL".}

\item{rowZoomLevel}{Numeric. Row zoom level between 0 and 1. Default is 1.}

\item{selectedIsDendrogram}{Logical. Whether selection is in dendrogram. Default is FALSE.}

\item{selectedStart}{Integer. Start position of selection (>= 0). Default is 0.}

\item{selectedStop}{Integer. End position of selection (>= 0). Default is 0.}

\item{version}{Character. Map version: "P" for primary, "S" for secondary. Default is "P".}

\item{versionNumber}{Integer. Detail map number for multiple detail panes. Default is 1.}
}
\value{
A new \code{detailMap} object with the specified properties
}
\description{
Creates a new detail map object that provides a zoomed-in view of a portion of the heatmap
with configurable display settings.
}
\examples{
# Create a basic detail map
detail <- detailMap(id = "pane1")

# Create a detail map with custom zoom and box size
detail2 <- detailMap(
  id = "pane2",
  colZoomLevel = 0.5,
  rowZoomLevel = 0.5,
  dataBoxHeight = 84,
  dataBoxWidth = 84
)

}
\seealso{
\itemize{
\item \linkS4class{detailMap} for class details
\item \link{panel_configuration} for using detail maps in configurations
}
}
