# Create a provid from a list of label values

Create a provid from a list of label values

## Usage

``` r
shaidyProvenance(...)
```

## Arguments

- ...:

  shaidyRepo followed by a list of name=value labels to store in the
  provid

## Value

A string containing the provid for the list of label values.
