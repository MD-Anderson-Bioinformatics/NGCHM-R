# List NG-CHM Servers

This function lists all NG-CHM (Next-Generation Clustered Heat Map)
servers that are currently available.

## Usage

``` r
chmListServers()
```

## Value

A character vector containing the names of all available servers.

## Examples

``` r
servers <- chmListServers() # Get a list of all available servers.
```
