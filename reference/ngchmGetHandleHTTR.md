# Get a HTTR handle for the server's view/WS URL

This function returns a 'handle' suitable for use with the server's
view/WS URL

## Usage

``` r
ngchmGetHandleHTTR(server)
```

## Arguments

- server:

  An object of class ngchmServer

## Value

An HTTR handle
